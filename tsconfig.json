// tsconfig.json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,

    /* ↓ add these two lines ↓ */
    "baseUrl": ".",                // allow absolute imports
    "paths": { "@/*": ["./*"] }    // "@/src/…" → "./src/…"
  }
}
